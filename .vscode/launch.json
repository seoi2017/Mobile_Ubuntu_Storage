{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch(Ubuntu)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/BASIC.exe",
            "targetArchitecture": "amd64",
            "miDebuggerPath":"/usr/bin/gdb",
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}",
            "externalConsole": true,
            "preLaunchTask": "g++"
        }
    ]
}